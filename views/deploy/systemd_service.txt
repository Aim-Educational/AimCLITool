$PLACEHOLDERS
    $NAME
    $WORKING_DIR
    $PORT
    $ENVIRONMENT_LIST
$END
$FINISH_CONFIG
[Unit]
Description=Service for AimDeploy project $AIM
After=network.target
StartLimitIntervalSec=0

[Service]
WorkingDirectory=$WORKING_DIR
ExecStart=/usr/bin/dotnet "$WORKING_DIR/$NAME.dll" --urls="http://localhost:$PORT"
Type=simple
Restart=always
RestartSec=10
KillSignal=SIGINT
User=root
$ENVIRONMENT_LIST

[Install]
WantedBy=multi-user.target